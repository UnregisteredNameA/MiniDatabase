A small database system. The details are described in the file "MiniSQL_guide".

Note that the default quotation mark used for char type is single quotation mark.
Use 
>>stringsign ";
to change to double quotation mark.

